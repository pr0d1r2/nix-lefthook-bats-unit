---
pre-commit:
  commands:
    bats-unit:
      glob: "*.bats"
      run: timeout ${LEFTHOOK_BATS_UNIT_TIMEOUT:-120} bats --jobs "$(nproc)" {staged_files}

pre-push:
  commands:
    bats-unit:
      glob: "*.bats"
      run: timeout ${LEFTHOOK_BATS_UNIT_TIMEOUT:-120} bats --jobs "$(nproc)" {push_files}
